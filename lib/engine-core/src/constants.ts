/** Minimum adjudication+outcome signals before atom-level calibration (I3 grain rule). */
export const MIN_DENSE_SIGNAL = 3;

/** Cold-start prior weight when blending asserted vs observed frequency. */
export const CALIBRATION_PRIOR_WEIGHT = 1;
