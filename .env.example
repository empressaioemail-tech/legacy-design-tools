# Example env for cortex-api / api-server (copy relevant vars to .env.local).
# See docs/deploy.md for production Secret Manager bindings.

# --- Property Brief / investor radar ---
# BROKERAGE_EXTENSION_PUBLIC_KEY=   # Chrome Web Store public key (required for /brief)
# BROKERAGE_OPERATOR_API_KEYS=      # optional operator/steward keys (GTM internal + operator tier)
# BROKERAGE_API_KEYS=               # optional external MCP caller keys (comma-separated)
# BROKERAGE_FEDERAL_DATA_DIR=       # live OZ + SPDPID data (Docker: /app/var/brokerage-federal-data)
# BROKERAGE_FEDERAL_DATA_GCS_PREFIX= # optional GCS prefix for boot hydration (/bucket/public/brokerage-federal-data)
# OZ_TRACT_DATA_PATH=
# TX_SPECIAL_DISTRICTS_DATA_PATH=
