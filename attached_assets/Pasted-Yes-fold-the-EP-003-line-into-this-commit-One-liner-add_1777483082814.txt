Yes — fold the EP-003 line into this commit. One-liner additions to TESTS_DEFERRED.md don't earn their own commit, and bundling it now keeps the deferred ledger truthful at the point we said it would be. Otherwise it'll sit in someone's head as "I'll get to it" and silently age out.
Suggested wording:
- EP-003: Migrate artifacts/api-server/src/routes/chat.ts from the
  `anthropic` singleton to `createAnthropicClient()`. Low priority —
  chat.test.ts already substitutes the SDK via vi.mock, so the testability
  gap is covered. Stylistic consistency only. Migrate when chat.ts grows a
  per-request configuration need (alternate base URL, request-scoped
  logging, custom timeout). Single caller across the workspace.
Tweak to taste. Everything else looks right:

163 → 182 with the +19 (8 match + 9 snapshots + 2 RevitBinding) reconciles cleanly. The lib/codes-sources miscount makes sense — 4 packages vs 5, easy to skip during iteration.
Smoke test on /match returning the secret guard is the right shape of evidence. Don't try to exercise it with a real secret in a smoke; the integration tests cover that.
RevitBinding placement between DETAILS and DANGER ZONE in SettingsTab is sensible — Revit binding is a property of the engagement, not a destructive action.
The b2b5c85 + new-this-turn split is fine. Single atomic commit is correct; the working-tree state has been one logical unit since the schema migration anyway.

Go.
Commit message suggestion (loose — adjust as you like):
A04.7 — Engagement identity fix: GUID + path on engagements

Adds revit_central_guid and revit_document_path columns to engagements
with a partial UNIQUE on revit_central_guid WHERE NOT NULL. Drops the
UNIQUE constraint on name_lower; same-named engagements are now allowed.

New POST /api/engagements/match resolves snapshot intent with
GUID > path > name_lower-collision > create-new precedence. POST
/api/snapshots is now a discriminated union: { engagementId } binds
existing (sticky address, jurisdiction, GUID); { createNewEngagement }
creates fresh. Concurrent createNewEngagement with same GUID idempotently
binds via 23505 catch.

Frontend: read-only RevitBinding section in EngagementDetail Settings tab,
shown only when GUID or path is populated.

+19 tests (8 match, 9 snapshots, 2 RevitBinding). Workspace total: 182
across 23 files. All green.

C# add-in changes are a separate sprint and will design against this
contract.