A04.7 — Engagement Identity Fix (legacy-design-tools, TypeScript monorepo)

Recon already complete (your earlier diagnostic). Approach approved: dropdown as
primary user-facing identity + Revit central file GUID as silent stability
backstop. Path as secondary fallback for non-workshared files.

DECISIONS (locked, don't re-litigate):
1. Address on re-bind: STICKY. Existing engagement's address/jurisdiction never
   auto-update from snapshot payload. Edits flow through engagement-edit UI only.
2. Dropdown trigger: ALWAYS on name_lower collision, regardless of GUID
   presence. The only silent auto-bind path is exact-GUID-match (or
   exact-path-match for non-workshared files).
3. Migration: existing engagements are test data, dismissable. No backfill.
   Destructive schema change is fine — drop the name_lower UNIQUE constraint.

ARCHITECTURE:
- engagements table: add revit_central_guid TEXT NULL, revit_document_path TEXT
  NULL. Partial UNIQUE index on revit_central_guid WHERE NOT NULL. Drop UNIQUE
  on name_lower; keep it as a plain index for search.
- New endpoint: POST /api/engagements/match
    body: { projectName, revitCentralGuid?, revitDocumentPath? }
    returns one of:
      { action: "auto-bind", engagementId, engagementName }
      { action: "choose", candidates: Engagement[] }
      { action: "create-new" }
    Precedence: GUID exact → document path exact → name_lower collision → none.
    Auth: same x-snapshot-secret pattern as /api/snapshots.
- POST /api/snapshots: contract change. No more auto-create from projectName
  alone. Body must be one of:
      { engagementId, ...sheets }
      { createNewEngagement: true, projectName, revitCentralGuid?,
        revitDocumentPath?, ...sheets }
  Reject 400 if neither shape is present. On createNewEngagement, persist GUID
  + path on the new engagement row.
- Frontend (artifacts/design-tools): engagement detail shows revitCentralGuid
  (truncated, monospace) and revitDocumentPath when present. No dropdown in
  frontend — that lives in the C# add-in, which consumes /match before upload.

IMPLEMENTATION FLOW:
[PLAN gate] Before any code, post a plan covering:
  - Drizzle migration shape (column adds, partial unique index, drop UNIQUE on
    name_lower) and whether it runs clean on the dev DB
  - Zod contract changes in lib/api-zod (CreateSnapshotBody is a breaking
    change; new MatchEngagementBody + MatchEngagementResponse discriminated
    union)
  - Route handler for /api/engagements/match — precedence logic, error cases,
    auth
  - Route handler changes for /api/snapshots — payload discrimination,
    validation, errors
  - Test plan per H01 patterns: route integration tests for /match covering all
    three response shapes, /snapshots covering both payload shapes + 400 case,
    schema test confirming partial UNIQUE works and name collisions are now
    allowed at the engagement level
  - Frontend changes (engagement detail additions only, plus any test updates)
  Pause for approval.

[EXECUTE]
  1. Drizzle migration. Verify it runs clean on dev DB.
  2. Contract additions in lib/api-zod.
  3. /api/engagements/match handler + tests.
  4. /api/snapshots handler refactor + tests.
  [INTERNAL GATE] Backend build green, new tests pass, full repo suite still
  163+ green. Pause if any pre-existing test breaks.
  5. Frontend engagement detail additions + tests.
  6. pnpm test full workspace, confirm green.

[REPORT]
  - Migration applied, schema verified (DESCRIBE engagements output)
  - Test counts before/after, all green
  - Contract diff summary — what /api/snapshots callers must change
  - Anything deferred, diverged, or surprising

INVARIANTS TO PRESERVE:
- buildChatPrompt is canonical chat assembler — don't bypass
- [[CODE:atomId]] citation format unchanged
- @workspace/db/testing patterns for any new schema fixtures
- Existing 163 tests must remain green
- Atom retrieval / Compass behavior unaffected — this sprint is purely
  ingestion-side

OUT OF SCOPE:
- C# add-in changes (separate sprint, parallel agent, will design against the
  live contract this sprint produces)
- Loom demo capture (after both sprints land)
- A06 reasoning loop
- Roadmap doc updates

Begin with the PLAN. No code yet.